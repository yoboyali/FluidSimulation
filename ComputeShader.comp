#version 460 core
layout(local_size_x = 16, local_size_y = 16) in;
layout(rgba32f, binding = 0) uniform image2D screen;

void main() {
    ivec2 coord = ivec2(gl_GlobalInvocationID.xy);
    imageStore(screen, coord, vec4(1.0, 0.0, 0.0, 1.0)); // red
}